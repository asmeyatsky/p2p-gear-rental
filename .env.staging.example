# Staging Environment Configuration
# Copy this file to .env.staging and fill in the values

# Staging URL (via load balancer)
STAGING_URL=https://smeyatsky.com/gear-staging

# Direct Cloud Run URL (for direct access testing)
# STAGING_DIRECT_URL=https://p2p-gear-rental-staging-737733013547.europe-west2.run.app

# Supabase Configuration (use staging/test project values)
NEXT_PUBLIC_SUPABASE_URL=your-supabase-project-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key

# Seed User Credentials (for API-based seeding)
# Create a test user in Supabase for seeding purposes
SEED_USER_EMAIL=seeduser@example.com
SEED_USER_PASSWORD=your-secure-password
